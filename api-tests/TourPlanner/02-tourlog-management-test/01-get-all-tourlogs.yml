info:
  name: 01-get-all-tourlogs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/tourLogs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
