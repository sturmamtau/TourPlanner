info:
  name: 04-delete-tourLog
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/tourLogs/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
