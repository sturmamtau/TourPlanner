info:
  name: 03-get-tour
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/tapi/tours/8"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
