info:
  name: 05-get-all-tours
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/api/tours"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
