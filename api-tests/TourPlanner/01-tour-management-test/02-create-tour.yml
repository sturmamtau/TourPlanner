info:
  name: 02-create-tour
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/tours"
  body:
    type: json
    data: |-
      {
        "Name": "new trip",
        "Description": "ORS call",
        "From": "Vienna",
        "To": "Salzburg",
        "TransportType": "Walk"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
