info:
  name: 02-create-tour
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/tours"
  body:
    type: json
    data: |-
      {
        "Name": "firsttour",
        "Description": "first tour in docker",
        "From": "yay",
        "To": "To",
        "TransportType": "Walk"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
