info:
  name: 02-create-tour
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/api/tours"
  body:
    type: json
    data: |-
      {
        "Name": "Tour5",
        "Description": "Description5",
        "From": "From5",
        "To": "To5",
        "TransportType": "Walk"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
