info:
  name: 01-get-all-tours
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/tours"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
