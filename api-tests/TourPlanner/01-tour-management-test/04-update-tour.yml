info:
  name: 04-update-tour
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/tours/2"
  body:
    type: json
    data: |-
      {
        "name": "Name-new",
        "description": "Des-new",
        "from": "From-new",
        "to": "To-new",
        "transportType": "Bike"
      }
  auth: inherit

runtime:
  assertions:
    - expression: ""
      operator: eq
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
