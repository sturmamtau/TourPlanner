info:
  name: 03-delete-tour
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/api/tours/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
